host: [127.0.0.1];
wordlist : /workspaces/projeto_compiladores/wordlist.txt;
openports: scan_tcp(host);
webPort: [80,443];


for port in openports {
    banner(port);
    case (port in webPort){
        enum(port,wordlist);
    }
}

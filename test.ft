host: 127.0.0.1; // Tipo: IP (NÃO ITERÁVEL)

wordlist: /path/to/common.txt; // Tipo: WORDLIST (ITERÁVEL)

//  ESTE DEVE GERAR ERRO SEMÂNTICO
for char in host {
    banner(char, host);
}

// ESTE DEVE PASSAR NA ANÁLISE SEMÂNTICA
for line in wordlist {
    banner(line, host);
}
